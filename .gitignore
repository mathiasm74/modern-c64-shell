# Build output
build/

# Delivery archive (its contents — CLAUDE.md, PLAN.md — are tracked directly)
files.zip

# Claude Code personal/local settings (shared config would be .claude/settings.json)
.claude/settings.local.json

# Throwaway disk copies the mutating disk tests (rm/cp) work on
test/data/_scratch*.d64

# Python
__pycache__/
*.pyc

# macOS
.DS_Store
